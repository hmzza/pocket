DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"
API_PORT=4000
WEB_URL="http://localhost:3000"
WEB_ORIGINS="http://localhost:3000,https://pocketpakistan.com,https://www.pocketpakistan.com"
API_URL="http://localhost:4000"
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="7d"
INITIAL_ADMIN_EMAIL="admin@pocketshawarma.com"
INITIAL_ADMIN_PASSWORD="PocketAdmin123!"
INITIAL_POS_EMAIL="counter@pocketshawarma.com"
INITIAL_POS_PASSWORD="PocketPos123!"

# Rider WhatsApp notifications.
# "deeplink" (default) prepares a wa.me link for an admin to open and send.
# "cloud-api" sends automatically and needs everything below, from Meta:
# a Business account, a verified WhatsApp number, a system-user token, and
# approved message templates. Business-initiated messages outside the 24-hour
# service window are rejected as free text, so the template names are required
# in practice, not optional.
WHATSAPP_PROVIDER="deeplink"
# WHATSAPP_TOKEN=""
# WHATSAPP_PHONE_NUMBER_ID=""
# WHATSAPP_API_VERSION="v21.0"
# WHATSAPP_TEMPLATE_RIDER_ASSIGNED=""
# WHATSAPP_TEMPLATE_RIDER_REVOKED=""
# WHATSAPP_TEMPLATE_LANGUAGE="en"
