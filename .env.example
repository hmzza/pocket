DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"
API_PORT=4000
WEB_URL="http://localhost:3000"
WEB_ORIGINS="http://localhost:3000,https://pocketpakistan.com,https://www.pocketpakistan.com"
API_URL="http://localhost:4000"
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="7d"
# Generate once with: npx web-push generate-vapid-keys --json
# Add the same three values to the production API environment. Never expose the private key.
VAPID_SUBJECT="mailto:hello@pocketshawarma.com"
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
INITIAL_ADMIN_EMAIL="admin@pocketshawarma.com"
INITIAL_ADMIN_PASSWORD="PocketAdmin123!"
INITIAL_POS_EMAIL="counter@pocketshawarma.com"
INITIAL_POS_PASSWORD="PocketPos123!"
